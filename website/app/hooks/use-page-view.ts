/**
 * @fileoverview Automatic page view tracking hook.
 *
 * Provides a simple hook that can be used in the future
 * for page view tracking. Currently a no-op.
 */

/**
 * Placeholder for page view tracking.
 * Analytics was removed along with the cloud SaaS backend.
 */
export function usePageView() {
  // No-op: analytics proxy endpoint was removed
}
