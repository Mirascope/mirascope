{
  "name": "mirascope-typescript",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "bun run --cwd mirascope typecheck && bun run --cwd examples typecheck",
    "build": "bun run --cwd mirascope build",
    "lint": "bun run typecheck && bun run lint:eslint && bun run format:check",
    "lint:eslint": "eslint .",
    "lint:eslint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun run --cwd mirascope test"
  },
  "workspaces": [
    "mirascope",
    "examples"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "eslint": "^9.29.0",
    "eslint-config-prettier": "^10.1.5",
    "jiti": "^2.4.2",
    "prettier": "^3.6.0",
    "typescript": "^5"
  }
}
